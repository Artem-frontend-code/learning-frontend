let arr = [4, 1, 7, 2, 5, 6 , 3];
//Чисто вспомнить это я напишу щас по памяти, если узнаю новое то будем коммент
for (let i = 1; i < arr.length; i++) {
    for (let j = 0; j < arr.length - i; j++) {

        if (arr[j] > arr[j+1]) {
        let temp = arr[j];
        arr[j] = arr[j+1];
        arr[j+1] = temp;
    }

    }
    
}

console.log(arr);