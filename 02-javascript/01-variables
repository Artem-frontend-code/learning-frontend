// ======ПЕРЕМЕННЫЕ=====

//let объявляет переменную
let userName="Artem"; //string тип
// Каждое последующее слово с заглавной буквы - верблюжья нотация
let userNameVip="Shadowfiend";

//Регистр имеет значение
let templarAssasin=10;
let TemplarAssasin=12;
console.log(templarAssasin,TemplarAssasin);

//const - неизменяемая переменная
const BIRTHDAY="2000.01.01";

//typeof - функция которя показывает тип переменной
let namee="dima",x=48;
console.log(typeof(namee),typeof(x),namee+5,x+5) //typeof унарный оператор и принимает одно значение

let xvx=true; //булевые значения boolean


/*
typeof (undefined)  "undefined"

typeof (10n) "bigint"- работает с числами выходящие за диапазон 64 бит

typeof (Symbol("id")) "symbol"

typeof Math  "object"  

typeof null  "object"  - ошибка js, null отдельный тип

typeof alert /"function" - ошибка js, функция сама по себе объект
*/

// Преобразует не числа в числа унарный +
console.log( +true ); // 1
console.log( +"" );   // 0

//Инкремент
let count=2;
count++;
console.log(count)
//Декремент
let count_2=2;
count_2--;
console.log(count_2)
//префиксная форма --count_2 уменьшает на 1 и возвращает новое значение
//Пост префиксное count++ - увеличивают переменную на один и возвращают старое
let counter = 1;
let a = ++counter; 
let b = counter++
console.log(a,b,counter)